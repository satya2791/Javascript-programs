// Program to print the multiplication table of a given number
// Initialize the number for which the multiplication table is to be printed
let n = 9;
// Loop through 1 to 10 and print the multiplication table
for (let i = 1; i <= 10; i++) {
    console.log(`${n} * ${i} = ${n * i}`);
}